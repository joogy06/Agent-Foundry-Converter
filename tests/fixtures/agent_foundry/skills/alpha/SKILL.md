---
name: alpha
description: Use when testing foundry-loader skills parsing. Sample skill alpha with a reference to `beta` skill.
---

# alpha

A fixture skill that references `beta` and uses the `Read` tool.

The workspace root is `~/.claude/skills/alpha/`.
